Sube los cambios del proyecto al repositorio GitHub de Newrest EPP.

Ejecuta exactamente esto:

```bash
cd "c:/Users/PC/Documents/PCS JUEGO/Juego epps" && git add . && git commit -m "Actualización del proyecto" && git push origin main
```

Si el commit falla porque no hay cambios nuevos, informa al usuario que el proyecto ya está actualizado en GitHub. Si el push es exitoso, confirma con el link: https://github.com/Beerjews/juegospcs
